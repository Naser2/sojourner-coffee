import Head from 'next/head'
import Image from 'next/image'
import NoContent from '../components/NoContent'

export default function Menu() {
  return (
    <div>
    <NoContent message={{text:"Menu is under contruction", code: 503, theme:"periwinkle"} } />
    </div>
  )
}
